<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mongoose Store</title>
</head>
<body>
    <nav><a href="/store">Back to products</a></nav>
    <h1><%=product.name%></h1>
    <div>
        <img src="<%=product.img%>" alt="" width="150px">
        <p><%=product.description%></br>PRICE: $<%=product.price%></p>
        <% if (product.qty>0){ %>
        <h3><%=product.qty%> remaining</h3>
        <form action="/store/<%=product.id%>?_method=POST" method="POST">
            <input type="submit" value="BUY">
        </form>
        <% }else{ %>
            <h3>OUT OF STOCK</h3>
            <% } %>
        <a href="/store/<%=product.id%>/edit">Edit this product</a>
    </div>
    <form action="/store/<%=product.id%>?_method=DELETE" method="POST">
		<input type="submit" value="DELETE" />
	</form>
</body>
</html>